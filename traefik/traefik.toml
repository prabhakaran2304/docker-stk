debug = false

logLevel = "ERROR"
defaultEntryPoints = ["http"]

[entryPoints]
    [entryPoints.http]
    address = ":80"

[retry]

[docker]
endpoint = "unix:///var/run/docker.sock"
domain = "sysadm.com"
watch = true
swarmmode = false
